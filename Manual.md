# คู่มือการใช้งานโปรแกรม PictureGPS Map
**ผู้สร้าง (Creator):** penthammachat

---

## 📌 แนะนำโปรแกรม (Overview)
**PictureGPS Map** เป็นเว็บแอปพลิเคชันที่ถูกออกแบบมาเพื่อใช้นำเข้ารูปภาพถ่าย (ที่มีข้อมูลพิกัด GPS) มาแสดงผลเป็นเส้นทางบนแผนที่แบบอินเทอร์แอกทีฟ โดยทำงานบนเบราว์เซอร์ของคุณ 100% (Client-side) ทำให้มีความเป็นส่วนตัวสูง รวดเร็ว และไม่ต้องอัปโหลดรูปภาพขึ้นเซิร์ฟเวอร์ใดๆ

โปรแกรมนี้เหมาะสำหรับสายเดินทาง, นักสำรวจ, หรือใครก็ตามที่ต้องการสร้างเส้นทางการเดินทางจากภาพถ่าย และส่งออก (Export) เป็นไฟล์มาตรฐาน (GPX/KML) เพื่อนำไปใช้งานต่อกับอุปกรณ์ GPS หรือ Google Earth

---

## ✨ ฟีเจอร์หลัก (Key Features)
1. **ระบบลากแล้ววาง (Drag & Drop):** นำเข้ารูปภาพได้อย่างง่ายดาย
2. **การอ่านค่า EXIF อัตโนมัติ:** ดึงข้อมูลพิกัดละติจูด-ลองจิจูด, วันที่และเวลา, และรุ่นกล้องจากรูปภาพโดยอัตโนมัติ
3. **การจัดการเส้นทาง:** สามารถจัดเรียงลำดับรูปภาพใหม่ได้ (ลากสลับตำแหน่ง) ซึ่งเส้นทางบนแผนที่จะอัปเดตตามทันที
4. **ไทม์ไลน์ (Timeline Slider):** แถบเลื่อนด้านล่างสำหรับดูรูปภาพตามลำดับการเดินทาง
5. **Lightbox:** คลิกที่รูปภาพเพื่อขยายดูรูปขนาดใหญ่ได้ทันที
6. **นำเข้าข้อมูล (Import):** รองรับการนำเข้าไฟล์เส้นทาง `.gpx` และ `.kml` เพื่อนำมาแสดงซ้อนกับรูปภาพ
7. **ส่งออกข้อมูล (Export):** บันทึกเส้นทางและรูปภาพออกมาเป็นไฟล์ GPX, KML, และ CSV
8. **บันทึกหน้าจอ (Screen Capture):** ฟังก์ชันจับภาพหน้าจอแผนที่หรือทั้งแอปเพื่อแชร์ให้เพื่อนได้ทันที

---

## 🛠️ วิธีการใช้งาน (Step-by-Step Guide)

### 1. การนำเข้ารูปภาพ (Importing Photos)
*   **วิธีที่ 1:** ลากไฟล์รูปภาพ (`.jpg`, `.jpeg`, `.png`, `.heic`) จากโฟลเดอร์ในคอมพิวเตอร์ของคุณ มาวางลงในพื้นที่ฝั่งซ้ายของโปรแกรม (บริเวณแถบรายการรูปภาพ)
*   **วิธีที่ 2:** หรือคลิกปุ่ม **"เลือกรูปภาพ..."** แล้วเลือกรูปภาพที่ต้องการ (เลือกได้หลายรูปพร้อมกัน)
*   *หมายเหตุ: โปรแกรมจะกรองเฉพาะรูปภาพที่มีการบันทึกพิกัด GPS ไว้เท่านั้น หากรูปภาพไหนไม่มีพิกัด จะมีระบบแจ้งเตือนให้ทราบ*

### 2. การดูข้อมูลและแผนที่ (Viewing Map & Data)
*   เมื่อโหลดรูปภาพเสร็จ โปรแกรมจะปักหมุด (Marker) บนแผนที่ตามพิกัดของแต่ละรูป พร้อมลากเส้นเชื่อมต่อกันเป็นเส้นทาง
*   **คลิกที่หมุดบนแผนที่:** จะมีหน้าต่าง (Popup) เล็กๆ เด้งขึ้นมาแสดงรูปภาพ, ลำดับ, เวลา, พิกัด และรุ่นกล้อง
*   **ขยายดูรูปเต็ม (Lightbox):** หากต้องการดูรูปชัดๆ ให้คลิกที่ "รูปภาพขนาดเล็ก" ใน Popup หรือในแถบรายการด้านซ้าย รูปภาพจะถูกขยายให้เต็มจอ (คลิกพื้นหลังสีดำเพื่อปิด)
*   **หมุดที่รวมตัวกัน (Clustering):** หากมีรูปภาพกระจุกตัวอยู่ในบริเวณเดียวกันมากเกินไป แผนที่จะรวบตัวเลขหมุดไว้ด้วยกัน (เช่น สีเขียวมีเลข 10) ให้คลิกหรือซูมเข้าไปเพื่อดูหมุดที่ซ่อนอยู่

### 3. การปรับแต่งลำดับและการลบรูปภาพ
*   **สลับลำดับรูป:** หากลำดับเส้นทางไม่ถูกต้อง คุณสามารถ "คลิกค้าง" ที่แถบรูปภาพทางด้านซ้าย แล้วลากขึ้น-ลง เพื่อสลับลำดับได้ เส้นทางบนแผนที่จะลากใหม่ตามลำดับที่คุณจัดทันที
*   **ลบรูป:** กดปุ่ม **"ถังขยะ"** (ลบ) ที่อยู่บริเวณมุมขวาบนของการ์ดรูปภาพแต่ละใบ เพื่อเอาออกจากเส้นทาง
*   **การแก้ไขวันที่:** สามารถกดที่ไอคอนรูปดินสอข้างๆ วันที่ในการ์ดรูปภาพ เพื่อแก้ไขวันที่และเวลาได้

### 4. การใช้แถบไทม์ไลน์ (Timeline)
*   ด้านล่างสุดของแผนที่จะมีแถบ "Timeline" (แถบเลื่อน)
*   คุณสามารถนำเมาส์ไปชี้หรือเลื่อนแถบ (Slider) ซ้าย-ขวา เพื่อไล่ดูตำแหน่งของรูปภาพตามลำดับการเดินทางได้ง่ายๆ โดยแผนที่จะเลื่อนตามอัตโนมัติ

### 5. การนำเข้าไฟล์เส้นทาง (Import GPX/KML)
หากคุณมีไฟล์ GPS เดิมอยู่แล้ว และต้องการนำรูปมาทาบทับลงไป:
*   ให้ลากไฟล์ `.gpx` หรือ `.kml` มาวางในโปรแกรม 
*   โปรแกรมจะวาดเส้นทาง (Track) สีน้ำเงินลงบนแผนที่
*   หลังจากนั้นคุณสามารถเพิ่มรูปภาพเข้าไปได้ รูปภาพจะกลายเป็น "หมุด" ที่เกาะอยู่ตามเส้นทางเหล่านั้น

### 6. การส่งออกข้อมูล (Exporting Data)
เมื่อคุณจัดเส้นทางเสร็จแล้ว สามารถกดปุ่มที่เมนูด้านบนขวาเพื่อส่งออกข้อมูลนำไปใช้ต่อได้:
*   **GPX:** ส่งออกเป็นไฟล์เส้นทางมาตรฐาน นำไปเปิดในเครื่องนำทาง Garmin, Strava ฯลฯ
*   **KML:** ส่งออกเป็นไฟล์สำหรับเปิดดูใน Google Earth หรือ Google Maps
*   **CSV:** ส่งออกตารางข้อมูล (ลำดับ, พิกัด, เวลา, ชื่อไฟล์) เพื่อนำไปเปิดใน Excel

### 7. การล้างข้อมูลทั้งหมด
หากต้องการเริ่มต้นใหม่ ให้กดปุ่ม **"🗑️ ล้างทั้งหมด"** ที่แถบเมนูด้านบนขวา โปรแกรมจะเคลียร์แผนที่และรูปภาพทั้งหมดออกทันที

---

## 📱 การใช้งานบนมือถือ (Mobile Usage)
โปรแกรมรองรับการใช้งานบนสมาร์ทโฟนเต็มรูปแบบ:
*   **แนวตั้ง (Portrait):** แผนที่จะอยู่ด้านบน และรายชื่อรูปภาพจะอยู่ด้านล่าง เพื่อให้กดใช้งานได้ง่าย
*   **แนวนอน (Landscape):** หน้าจอจะแบ่งซ้าย-ขวา คล้ายคลึงกับบน Desktop
*   การเลื่อนสลับลำดับรูปภาพ สามารถใช้นิ้วแตะค้างแล้วลาก (Drag) ได้เช่นเดียวกัน

---
*© Copyright - PictureGPS Map by penthammachat*
